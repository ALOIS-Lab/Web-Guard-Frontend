# The URL of the backend API server. This is used by Vite's dev server to proxy /api requests.
VITE_API_URL

# Comma-separated list of allowed hostnames for the dev server (Vite 8 security allowedHosts)
VITE_ALLOWED_HOSTS
